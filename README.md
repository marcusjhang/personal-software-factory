# Agent Project Management

A deeply researched, plan-only design for a repository-native, factory-as-code agent operating system.

## Primary deliverable

Read **[PLAN.md](./PLAN.md)**.

It covers the complete ideation, engineering build, maintenance, deep-research, governed self-improvement, multi-repository dogfooding, and future GTM lifecycle.

## Supporting material

- [`research/repository-agent-operating-system/`](./research/repository-agent-operating-system/) — focused research, evidence, and scope decisions.
- [`research/self-managing-software-projects/`](./research/self-managing-software-projects/) — foundational landscape research.
- [`review/`](./review/) — adversarial findings, plan-contract resolution registers, and requirements traceability.
- [`docs/state-transition-manifests.md`](./docs/state-transition-manifests.md) — normative legal-edge manifests incorporated by `PLAN.md`; other `docs/` files are supporting drafts that `PLAN.md` supersedes on conflict.

## Status

This repository intentionally contains a plan, not a working controller or deployed factory. Prototype code used for feasibility testing was removed after its failures were incorporated into the plan.

Round 16 formal/status corrections and schema-3 candidate-only material-graph regeneration are complete for the current PLAN and normative-manifest bytes. All five independent round-17 closure audits pass with **0 critical and 0 high findings**, and the requirements audit maps **16/16** requirements. This remains a plan-only result: it does not prove implementation, exhaustive reachable-product model checking, legacy evidence provenance, semantic validity, or production safety. Immutable re-fetch, same-run human semantic review, deterministic reviewed-edge regeneration, and the implementation/model gates remain required before Increment 1.

## Round-12 formal correction status

All **8/8 raw high findings** across all **5/5** `review/round-12-closure/*.json` files map individually in `review/round-12-resolution.md`. The formal edits cover improvement rollback-safety block release after independent verification, exact EVIDENCE_STALE widening/promotion Effect continuation, exposed-CANARY_PASSED cancellation, a reachable-tuple-derived control-degradation exposure set, BUILD/MAINTENANCE first cancellation after `ROLLED_BACK`, and historical checkpoint wording.

The round-11 candidate-only graph checkpoint is historical. The round-12 schema-3 graph matched the then-current round-12 PLAN/manifest digests and all 28 dynamically derived headings; round 13 superseded that checkpoint. This status does not claim closure, implementation, full model proof, semantic validation, or production safety.

## Round-13 formal correction status

All **3/3 raw high findings** across all **5/5** `review/round-13-closure/*.json` files map individually in `review/round-13-resolution.md`; they represent **2 unique formal defects**. The correction adds exact HEALTHY-to-degraded interrupts for already-reconciling unsettled widening/promotion Effects and makes IC24-RWX/RPX atomically leave reconciliation for legal rollback dispatch with exact immutable origins. Exact reachable-interrupt equality and before/after ordering witnesses were updated.

These were plan-contract edits only. The round-12 graph checkpoint is historical; the round-13 schema-3 graph matched the then-current round-13 PLAN/manifest digests and all 28 headings. The round-14 schema-3 graph matched the then-current round-14 PLAN/manifest digests and all 28 headings; round 15 superseded that checkpoint. No implementation, exhaustive model proof, semantic evidence validation, production safety, or independent closure is claimed.

## Round-14 formal correction status

All **5/5 raw high findings** across all **5/5** `review/round-14-closure/*.json` files map individually in `review/round-14-resolution.md`; they represent **5 unique defects**. The formal correction makes release-attempt cancellation subordinate to whole-work IX cancellation, adds the complete protected-X rollback-adoption degradation partition, makes the interrupt union literal and complete, separates IH06 from IX cancellation continuations, and corrects historical checkpoint wording.

These are historical formal/status corrections. The round-14 source bytes and schema-3 candidate graph matched at that checkpoint; round-15 formal bytes supersede them. No implementation, exhaustive model proof, semantic evidence validation, production safety, or independent closure is claimed.


## Round-15 formal/status correction status

All **6/6 raw high findings** across all **5/5** `review/round-15-closure/*.json` files map individually in `review/round-15-resolution.md`; they represent **4 unique defects**. The correction closes release-attempt failure-cleanup cancellation, cancellation-first degradation recording, IX revocation of `FROZEN|EVIDENCE_STALE`, and the round-14 graph-byte status contradiction.

These are historical formal/status corrections only. The round-15 formal bytes and matching schema-3 candidate graph matched at that checkpoint; round-16 formal bytes supersede them. Round-15 generated metadata, source digests, checkpoint, and graph bytes were regenerated without changing source-evidence records or edge semantics. No implementation, exhaustive model proof, semantic evidence validation, production safety, or independent closure is claimed.


## Round-16 formal/status correction status

All **4/4 raw high findings** across all **5/5** `review/round-16-closure/*.json` files map individually in `review/round-16-resolution.md`; they represent **3 unique defects**. The correction adds REQUESTED/QUIESCING revocation ownership for already-reconciling unsettled Effects, partitions cancellation-side degradation into active-generation and constructible no-active-generation receipt coordinates, and makes historical graph status exact.

These are formal/status corrections only. The current formal bytes and matching schema-3 candidate graph are round 16 and cover all 28 dynamically derived headings. No implementation, exhaustive model proof, semantic evidence validation, production safety, or independent closure is claimed.

## Round-17 closure status

All five independent audits in [`review/round-17-closure/`](./review/round-17-closure/) pass with **0 critical and 0 high findings** against the current round-16 PLAN, manifest, and schema-3 candidate graph. The formal audit reproduced **801 unique rows** (**96 canonical**, **705 private/product**), and the evidence/requirements audit reproduced **16/16** requirement mappings. See [`review/round-17-closure-summary.md`](./review/round-17-closure-summary.md).

This closes the plan-contract review only. The explicitly gated future work—fresh immutable evidence retrieval and same-run human semantic review, implementation, exhaustive generated-model execution, PostgreSQL/RLS proofs, production validation, and protected improvement promotion—remains unperformed and is not claimed.
