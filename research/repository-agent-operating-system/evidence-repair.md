# Evidence repair status and candidate proposition inventory

**Scope:** plan-only evidence audit for `EVID-R2-001`, `EVID-R2-002`, and later closure review.
**Controlling decision cutoff:** `2026-09-12T23:59:59Z`.
**Status:** No immutable re-fetch or human semantic review is claimed.

## Controlling provenance boundary

The earlier UTC+08/local-date explanation is only an **unverified legacy process narrative**. Labels such as `access_date`, `as_of`, “accessed,” “observed,” or “snapshot accessed” establish no host timezone, actual UTC retrieval time, or compliance with the controlling cutoff.

A fresh immutable re-fetch plus human semantic review is a **PRE-IMPLEMENTATION** gate that blocks Increment 1. It is not implementation-time work. Until that gate passes, legacy source provenance and semantic entailment remain unresolved.

## Completed editorial artifacts

- `research-manifest.json` inventories exactly 37 checked-in result files: 14 self-managing-software-projects, 10 RAOS base, 8 lifecycle extension, and 5 domain extension.
- `synthesis-coverage.md` records one coverage entry per research track and preserves conclusions, uncertainties, conflicts, proposed uses, and repeated evidence families.
- `material-claims.json` is finally regenerated for its current recorded digest. This is an editorial claim-graph completion, not provenance or semantic closure.
- `evidence-index.md` provides a bounded source-field inventory for the 23 focused RAOS, lifecycle, and domain tracks.

These artifacts establish local byte identity, parseability, field presence, and editorial linkage only. Legacy structure does not semantically establish external content. It does not establish source truth, source independence, authenticity, retrieval time, cutoff compliance, current product behavior, or local efficacy.

## Unverified candidate proposition inventory

The following items are candidate propositions for the future immutable run and human entailment review. They are **unverified candidates only**, and their provenance and semantic entailment are unresolved.

1. **Structural corpus fact:** 37 declared result JSON files exist and parse at this checkout, divided into 14 SMSP, 10 RAOS base, 8 lifecycle-extension, and 5 domain-extension tracks.
2. **GitHub capability candidates:** legacy records associate GitHub documentation with Projects API mutation, webhook controls, GitHub App permissions and tokens, rulesets, CODEOWNERS, merge queue, environments, Actions security guidance, concurrency modes, and artifact attestations.
3. **Standards/guidance candidates:** legacy records associate source URLs with JSON Schema, RFC 8785, CloudEvents, SLSA, OpenTelemetry, NIST SSDF, OWASP agent risks, OpenGitOps principles, and canary practices.
4. **Concurrency inference proposal:** the plan proposes that documented queue primitives are insufficient by themselves for its renewable-lease, fencing, transactional-admission, and dependency-aware controller. This remains an architecture inference, not an external fact established by corpus structure.
5. **METR candidate finding:** the legacy inventory links one METR paper/blog evidence family to a narrow transfer and perception caution. The numerical and semantic characterization requires fresh observation and human review and must not be treated as two independent sources or a universal productivity result.
6. **Component-to-composition boundary:** the plan proposes a composition from candidate primitives. Neither the inventory nor the claim graph demonstrates the composition’s end-to-end safety, effectiveness, productivity, ROI, or universal transfer.
7. **Governance architecture proposals:** authority separation, leases, reconciliation, cleanup, evaluation, staged promotion, rollback, and protected evidence are plan proposals. They remain proposals even when legacy records contain similarly named concepts.

## Propositions excluded pending review or local evidence

The current artifacts do not establish:

- a verified pre-cutoff retrieval timeline for any legacy source;
- that a `2026-09-13` label records UTC+08 collection or any other host timezone;
- that all 37 results are provenance-validated or semantically validated;
- that URL presence, a title, or a populated citation object entails a claim;
- independent replication when multiple records reuse one publisher, study, benchmark, or documentation family;
- production safety, correctness, productivity, ROI, or correctness of the proposed system;
- safe general recursive self-improvement;
- universal numeric thresholds, risk tiers, timeouts, lease durations, or promotion criteria;
- complete current behavior of mutable products or documentation;
- causal efficacy from vendor-reported case studies without comparable denominators and independent review.

## Structural versus provenance and semantic validation

Completed deterministic checks cover:

- expected path and file counts;
- JSON parsing and required field shape;
- local SHA-256 byte identity;
- track-to-plan mapping shape;
- claim-ID and edge-schema consistency;
- URL membership and duplicate-family bookkeeping;
- index shape and source-field comparison.

They do not cover:

- actual UTC retrieval observation;
- response authenticity or retained immutable source bytes;
- whether a cited source says what a candidate proposition attributes to it;
- whether qualifiers, dates, conflicts, denominators, and transfer limits are complete;
- whether a source was current and eligible at the controlling cutoff;
- whether architecture inferences are sound;
- whether proposed local outcomes will occur.

Thus provenance and semantic entailment remain unresolved for every external-content edge until a human reviews the fresh immutable observations.

## Conflict and evidence-family inventory

The future review must preserve, rather than average away:

- early METR slowdown versus later weak or uncertain estimates and perceived speedup;
- vendor delivery/merge claims versus missing assigned-task denominators and independent failures;
- static benchmark scores versus leakage, weak-test, and fresh-task degradation concerns;
- vendor RCA claims versus low adversarial benchmark results;
- AI review usefulness versus recall gaps and no demonstrated time saving;
- multi-agent benchmark gains versus coordination failures and simpler competing pipelines;
- deterministic remediation claims versus weak evidence for novel autonomous repair;
- dependency-automation value versus scanner noise, major-update risk, and acceptance-not-correctness;
- platform capability candidates versus absent controller-level transaction semantics;
- broad availability labels versus evidence of efficacy.

Repeated families include the METR paper/blog, SWE-bench and related audits/variants, DORA/SPACE frameworks, AgentDojo-related benchmarks, GitHub documentation families, OWASP guidance, Temporal documentation, SLSA/in-toto, and the `tj-actions/changed-files` incident family. A repeated family must not inflate an independent-source count.

## Superseded editorial instructions

The former exact-edit block and its deferred-docs instructions are **superseded and completed**. They are historical repair notes, not current work. The current plan already contains the evidence boundary and the PRE-IMPLEMENTATION gate. No later PLAN edit or material-claim regeneration is requested by this file.

The current claim IDs are:

`MC-002`, `MC-003`, `MC-004`, `MC-005`, `MC-006`, `MC-007`, `MC-008`, `MC-009`, `MC-010`, `MC-011`, `MC-012`, `MC-013`, `MC-014`, `MC-018`, `MC-019`, `MC-020`, `MC-021`, `MC-022`, `MC-023`, and `MC-024`.

Supersession status:

- `MC-001` was removed and replaced by current `MC-021`.
- Prior versions of `MC-002`, `MC-012`, `MC-013`, and `MC-020` are superseded by their current versions.
- `MC-015`, `MC-016`, and `MC-017` were removed as bibliography-only overstatement.

The exact machine-readable dispositions in `material-claims.json` control if this summary and that file differ.

## Remaining blocking gate

Before Increment 1, perform one fresh immutable research run for every materially relied-on source. Record at least:

- immutable `run_id` and validator version;
- actual UTC `retrieved_at`;
- requested and resolved URL plus response status;
- content digest and retained snapshot, or an explicit snapshot-unavailable reason;
- canonical work, source-observation, and evidence-family IDs;
- the protected expected source/claim set and received-set completeness;
- human semantic disposition for each material edge, including qualifiers, conflicts, and transfer limits.

Reject future timestamps, missing material provenance, mixed or incomplete runs, unexpected or duplicate observations, hidden conflicts, and non-entailing edges. Then regenerate the report, matrix, coverage, and material claim graph from that same run.

## Closure checks

Closure requires:

1. exactly 37 manifested result paths and no unexplained outline omission;
2. one coverage entry per result track;
3. every material PLAN claim mapped to a current stable claim ID;
4. every external-content relationship human-reviewed against immutable source bytes;
5. duplicate-family collapse and explicit conflicts;
6. a complete same-run source and claim set with UTC observations and snapshot dispositions;
7. separate structural, provenance, semantic, and local-outcome statuses;
8. completion before Increment 1 begins.

Final material-claim regeneration is complete for the current digest recorded in that artifact. The distinct immutable re-fetch and human semantic review remain future PRE-IMPLEMENTATION work and continue to block Increment 1.
